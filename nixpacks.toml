[phases.setup]
nixPkgs = ['ffmpeg', 'python3', 'python311Packages.pip']

[phases.install]
cmds = ['npm install', 'python3 -m pip install yt-dlp']

[phases.build]
dependsOn = ['install']
cmds = ['cd frontend && npm install && npm run build']

[start]
cmd = 'node backend/server.js'
